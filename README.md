# mingalar_run

A Step Counter project.

## Getting Started


Development 

dart run build_runner build --delete-conflicting-outputs

dart run easy_localization:generate -S assets/translations -f keys -O lib/l10n -o locale_keys.g.dart